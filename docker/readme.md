# 🐳 Docker

This directory contains Docker configuration files used to build, run, and manage the application in a containerized environment.

Docker ensures the application runs consistently across development, testing, and production systems.

---